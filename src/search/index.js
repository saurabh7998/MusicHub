import Grid from "@mui/material/Grid";
import SideNavbar from "../shared/SideNavbar";

const Search = () => {
    return (
        <Grid container>
            <SideNavbar/>
        </Grid>
    );
}

export default Search;